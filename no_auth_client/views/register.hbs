<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Đăng ký tài khoản chứng khoán</title>
    <link rel="stylesheet" href="/styles/register.css" />
</head>

<body>
    <div class="container">
        <p style="text-align:start; margin-bottom: 1rem; color: #555;">
            Hiện tại đã có <strong>{{totalRegisters}}</strong> đơn đăng ký.
        </p>
        <h1>Đăng ký tài khoản chứng khoán cho sinh viên</h1>

        {{#if message}}
        <div class="message success">{{message}}</div>
        {{/if}}

        {{#if error}}
        <div class="message error">{{error}}</div>
        {{/if}}

        <form method="POST" action="/register">
            <label for="full_name">Họ tên</label>
            <input type="text" id="full_name" name="full_name" value="{{formData.full_name}}" required />

            <label for="email">Email</label>
            <input type="email" id="email" name="email" value="{{formData.email}}" required />

            <label for="phone">Số điện thoại</label>
            <input type="text" id="phone" name="phone" value="{{formData.phone}}" required />

            <label for="university">Trường đại học</label>
            <input type="text" id="university" name="university" value="{{formData.university}}" required />

            <button type="submit">Đăng ký</button>
        </form>
    </div>
</body>

</html>